version: 1
script:
  - cat <<EOF > chd_converter_launcher.sh
#!/bin/sh
HERE="\$(dirname "\$(readlink -f "\$0")")"
export PATH="\$HERE/usr/bin:\$PATH"
export PYTHONHOME="\$HERE/usr"
export PYTHONPATH="\$HERE/usr/lib/python3/dist-packages:\$PYTHONPATH"
exec "\$HERE/usr/bin/python3" "\$HERE/usr/src/chd_converter_app.py" "\$@"
EOF
  - chmod +x chd_converter_launcher.sh

AppDir:
  path: ./AppDir
  app_info:
    id: com.user.chdconverter
    name: CHD Converter
    icon: utilities-terminal
    version: 1.0.0
    exec: chd_converter_launcher.sh
    exec_args: "$@"

  apt:
    arch: amd64
    sources:
      - sourceline: 'deb http://archive.ubuntu.com/ubuntu/ jammy main universe'
      - sourceline: 'deb http://archive.ubuntu.com/ubuntu/ jammy-updates main universe'
    include:
      - python3
      - python3-tk
      - chdman
      - kdialog
      - zenity
    exclude: []

  files:
    include:
      - chd_gui_advanced.py

  runtime:
    env:
      PATH: '${APPDIR}/usr/bin:${PATH}'

AppImage:
  arch: x86_64
  update-information: None
  sign-key: None
